{
  "name": "sentinel-app-portfolio-manager",
  "version": "1.0.0",
  "type": "module",
  "description": "Sentinel - A centralized dashboard for managing app portfolio improvement, maintenance rhythm, and incubation",
  "main": "src/App.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:with-password": "npm run setup-password && vite build",
    "preview": "vite preview",
    "serve": "vite preview --port 3000",
    "start": "node server/index.js",
    "api": "node server/index.js",
    "setup-password": "node scripts/setup-password.js",
    "deploy": "npm run build && npm run deploy:gh-pages",
    "deploy:gh-pages": "gh-pages -d dist",
    "predeploy": "npm run build"
  },
  "keywords": [
    "portfolio",
    "app-management",
    "github-api",
    "dashboard",
    "technical-debt"
  ],
  "author": "Sentinel Team",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3"
  }
}
