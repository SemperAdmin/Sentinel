// Runtime configuration
// This file can be modified on the server without rebuilding
window.APP_CONFIG = {
  // Admin password - replace this value in production
  // On Render.com, you can use environment variable substitution
  ADMIN_PASSWORD: '${ADMIN_PASSWORD}' || 'admin123'
};
